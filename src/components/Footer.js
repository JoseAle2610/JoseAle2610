import styled from 'styled-components'

export const Footer = styled(({className}) => (
  <footer className={className}>
    <h3>© Copyright</h3> 
  </footer>
))`
  padding: 2em;
  text-align: center;
`
